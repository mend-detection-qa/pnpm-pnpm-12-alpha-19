# GitHub Actions package registry for @my-org scope.
# Used to demonstrate registry_source: private-index for scoped packages
# while workspace: protocol references remain source: local.
@my-org:registry=https://npm.pkg.github.com

# pnpm 12 strictness settings
strict-peer-dependencies=false
auto-install-peers=true
node-linker=isolated
